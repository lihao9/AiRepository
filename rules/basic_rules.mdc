# Android Basic Rules

## 语言与架构
- 默认使用 Kotlin，新代码优先放在清晰的分层结构中（UI / Domain / Data）。
- 单个类职责单一；超过 400 行或职责混杂时优先拆分。
- 公开 API（public 方法/类）必须有清晰命名与最小必要注释。

## UI 与状态管理
- 使用 Jetpack Compose
- UI 层不直接访问网络或数据库，通过 ViewModel/UseCase 间接访问。
- 所有 UI 状态必须可追踪（Loading / Success / Error），避免“隐式状态”。
- 项目内弹窗统一使用 Compose `Dialog` 实现，禁止用页面内叠层 `Box/Surface` 冒充弹窗。
- `Text` 组件禁止使用 `style` 属性，统一改用基础属性（如 `fontSize`、`fontWeight`、`color` 等）。
- 当文案存在下划线、局部颜色变化、局部点击等富文本需求时，优先使用 `buildAnnotatedString`。
- `buildAnnotatedString` 中每一段可见文案必须各自包一层 `withStyle`（含默认样式的正文段），禁止裸 `append` 无样式段落。
- 需要特殊颜色或下划线且可点击的片段必须使用 `withLink` + `LinkAnnotation`（如 `LinkAnnotation.Clickable` / `Url`）承载点击，禁止仅用 `withStyle` + 外层 `clickable` 冒充链接。
- 所有新增页面必须使用 `BaseCompose` 包裹，禁止页面直接裸写根容器。
- `BaseCompose` 必须基于 `Scaffold` 统一处理页面结构与通用层（加载、错误、提示等）。

## 页面创建输入门禁（Image-first）
- 触发范围：凡是“页面创建 / 页面实现 / 页面搭建 / 页面UI开发”类 skill 任务，均视为页面创建任务。
- 输入前置：执行页面创建类 skill 前，必须先提供且仅提供本地图片路径（截图或设计稿）。
- 缺失处理：若未提供图片，必须先要求补充图片；在收到图片前禁止生成 Compose 页面代码。
- 输入校验：若提供远程链接、或无效本地路径，必须要求用户改为 有效本地图后再继续。
- 生成要求：收到图片后，基于图片结构生成 Compose 界面（页面容器、组件层级、间距布局、交互区块、基础状态占位）。
- 状态要求：生成页面时必须明确 Loading / Success / Error / Empty（如适用）状态，禁止仅实现静态 UI。
- 架构要求：页面 UI 逻辑通过 ViewModel/UseCase 驱动，UI 层不得直接访问网络或数据库。
- 资源要求：用户可见文案统一放入 `strings.xml`；颜色、尺寸、间距优先复用现有主题与资源，避免硬编码与魔法数。

## 协程与线程
- 异步任务统一使用 Kotlin Coroutines。
- 耗时任务必须切到合适 Dispatcher（IO/Default），禁止阻塞主线程。
- 在 ViewModel 中使用结构化并发，避免 GlobalScope。

## 错误处理与日志
- 禁止吞异常（空 catch）；必须记录上下文并返回可处理结果。
- 用户可见错误给出可理解提示；开发日志包含错误原因与关键参数。
- 对外层（Repository/UseCase）返回统一 Result 类型（Success/Failure）。

## 资源与配置
- 文本统一放在 `strings.xml`，禁止硬编码用户可见文案。
- 颜色、尺寸、间距使用资源或主题统一管理，避免魔法数。
- 新增权限、Activity、Service 时同步更新 `AndroidManifest.xml` 并说明用途。

## 质量与测试
- 新增业务逻辑优先补单元测试；关键流程补最小可行 UI/集成测试。
- PR 至少通过编译与基础测试（如 `testDebugUnitTest`）。
- 变更包含：目的、影响范围、验证步骤（测试或手动验证）。

## 安全与隐私
- 禁止提交密钥、token、证书等敏感信息到仓库。
- 敏感数据使用安全存储；日志中禁止打印隐私数据。
- 网络请求默认使用 HTTPS，并处理证书/超时/重试策略。